/**
 * Backward-compatible re-export for path helpers.
 * Prefer importing from `lib/firebase/paths` directly.
 */
export {
  businessPath,
  productsPath,
  productPath,
  customersPath,
  customerPath,
  suppliersPath,
  supplierPath,
  paymentAgentsPath,
  paymentAgentPath,
  ordersPath,
  orderPath,
} from "./paths";
