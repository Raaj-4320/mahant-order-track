# Firebase services (Phase 3 scaffold)

This folder is reserved for Firebase-backed service implementations that will eventually satisfy
existing contracts in `services/contracts.ts`.

## Current status
- Not used by pages yet.
- Mock services remain the active data source.
- No runtime module migration in Phase 3A.

## Planned Phase 3B
- Implement Products Firebase service first.
- Add a safe data-source switch/feature flag.
- Keep all other modules on mock services until product module is stable.
